{
  "name": "voip-receptionist",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "format": "pnpm -r --if-present format"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "@types/uuid": "^11.0.0",
    "uuid": "^13.0.0",
    "twilio": "^5.3.4",
    "@aws-sdk/client-secrets-manager": "^3.705.0"
  }
}
