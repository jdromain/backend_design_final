# Copy to .env.local for local demo (see docs/setup.md)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_DEFAULT_TENANT_ID=test-tenant
# Clerk: set to false to use JWT dev-login while keeping keys in env
CLERK_ENABLED=true
