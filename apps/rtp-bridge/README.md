# rtp-bridge

Placeholder for the Go/Rust media bridge that handles RTP ↔ PCM and streams audio to the Node realtime-core process over gRPC or a UNIX domain socket. Implementations will land in later phases.

